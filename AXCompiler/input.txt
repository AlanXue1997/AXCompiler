int f(int a){
	if(a<2) return 1;
	else return f(a-1)*a;
}
int main(){
	int a[10];
	int A;
	for(A=0;A<10;A=A+1){
		printf("%d\n", f(A+1));
	}
	return 0;
}

/*
	不支持指针;
	不支持参数中存在数组;
	不支持一句话声明多个变量；
	不支持常量表达式；
	不支持重载；
	不支持函数声明；
	记得判断，数组不能是临时变量
*/
